# [RUN] 定向配置简报 — 本期关键变化速览
# 输入: 本期定向配置分析结果
# 输出: 2-5 条 [OK]/[WARN] 标注的观测结论

print(f"\n{'─'*50}")
print(f"[简报] 定向配置 — 2025-03 月度快照")
print(f"{'─'*50}")

# 每条观测：(状态标签, 描述文字)
# [OK]  = 正常/稳定/在预期内
# [WARN] = 有变化/需关注/超出预警阈值
takeaways = [
    ("[OK]",   "抖音渠道定向覆盖用户数环比 +2.1%，趋势平稳"),
    ("[OK]",   "V8精准排除包启用率维持 85% 以上，配置稳定"),
    ("[WARN]", "腾讯渠道新增 3 个广告组未挂载排除包，溢出风险上升"),
    ("[WARN]", "API排除包撞库名单本月未更新（距上次更新已 47 天）"),
]

for status, text in takeaways:
    print(f"  {status} {text}")

# 若存在 WARN 条目，给出一条行动建议
print(f"\n  -> 建议: 对腾讯未挂载广告组补充排除包配置，并触发撞库名单更新")
